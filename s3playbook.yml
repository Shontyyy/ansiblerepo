- hosts: localhost
  connection: local
  gather_facts: False
  vars:
    ansible_python_interpreter:  /usr/local/lib/python3.10/dist-packages
  tasks:
    - name: Create S3 bucket
      s3_bucket:
        name: my-bucket-09111996
        state: present
        region: ap-south-1
